import { adminTypes } from "./admin.types";
import { organizationTypes } from "./organization.types";
import { userTypes } from "./user.types";

const types = [adminTypes, organizationTypes, userTypes];

export default types;
