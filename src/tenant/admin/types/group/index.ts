import { approvalTypes } from './approval.types'
import { interestsTypes } from './interests.types'
import { settingTypes } from './settingsTypes'
import { themeTypes } from './theme.types'

const groupTypes = [interestsTypes, themeTypes, settingTypes, approvalTypes]

export default groupTypes
